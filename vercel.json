{
  "buildCommand": "prisma generate && next build",
  "installCommand": "bun install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "ignoreCommand": "bash -c '[[ ! $(git diff --stat HEAD^ HEAD -- . \":(exclude)py-server\") ]]'"
}
